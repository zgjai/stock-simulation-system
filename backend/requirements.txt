fastapi==0.104.1
uvicorn[standard]==0.24.0
pandas==2.1.3
numpy==1.26.2
pyyaml==6.0.1
python-multipart==0.0.6

# 数据库相关（SQLite 驱动是 Python 自带的，无需安装）
sqlalchemy==2.0.23

# 数据迁移工具
tqdm==4.66.1

# 云数据库相关（部署时需要，本地开发不需要）
# psycopg2-binary==2.9.9  # PostgreSQL
# python-dotenv==1.0.0  # 环境变量
# supabase==2.3.0  # Supabase 客户端
# redis==5.0.1  # 缓存
